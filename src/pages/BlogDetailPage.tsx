import { Navigate, useParams } from 'react-router-dom'
import { Breadcrumb } from '../components/common/Breadcrumb'
import { blogPosts } from '../data/content'
import { useSeo } from '../hooks/useSeo'

export default function BlogDetailPage(){const{slug}=useParams();const post=blogPosts.find((p)=>p.slug===slug);useSeo({title:post?.title??'Không tìm thấy bài viết',description:post?.excerpt??'Bài viết không tồn tại.'});if(!post)return <Navigate to="/404" replace/>;return <article className="bg-paper pb-24 pt-32 md:pt-40"><div className="container-site"><Breadcrumb items={[{label:'Blog',to:'/blog'},{label:post.title}]}/><header className="mx-auto mt-12 max-w-5xl text-center"><p className="eyebrow text-iris">{post.category} · {post.date}</p><h1 className="display text-balance mt-6 text-[clamp(3.5rem,9vw,7.5rem)]">{post.title}</h1><p className="mx-auto mt-7 max-w-2xl text-lg leading-8 text-ink/55">{post.excerpt}</p></header><img src={post.image} alt={post.title} className="mt-12 aspect-[16/8] w-full rounded-[2rem] object-cover"/><div className="mx-auto mt-14 max-w-2xl space-y-7 text-base leading-8 text-ink/70"><p className="font-display text-3xl leading-snug text-ink">Học một nhạc cụ không phải là cuộc đua. Đó là quá trình xây dựng một mối quan hệ bền vững với âm thanh.</p><p>Điều quan trọng nhất trong giai đoạn đầu không phải là chơi thật nhiều bài, mà là hình thành thói quen đúng: ngồi vào nhạc cụ đều đặn, lắng nghe chủ động và biết cách chia nhỏ một thử thách.</p><h2 className="font-display text-4xl font-semibold text-ink">Bắt đầu từ điều nhỏ nhất</h2><p>Chọn một mục tiêu đủ nhỏ để hoàn thành trong mỗi lần tập. Có thể là bốn ô nhịp, một lần chuyển hợp âm sạch hoặc giữ đúng groove trong 30 giây. Cảm giác hoàn thành sẽ tạo động lực cho buổi tiếp theo.</p><blockquote className="border-l-2 border-iris pl-6 font-display text-2xl italic text-ink">“Sự đều đặn luôn thắng những buổi tập thật dài nhưng hiếm hoi.”</blockquote><p>Giáo viên tốt không chỉ giao bài, mà còn giúp học viên hiểu mục đích của từng bài tập. Khi biết mình đang luyện điều gì, thời gian với nhạc cụ trở nên thú vị và có ý nghĩa hơn.</p></div></div></article>}
